/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Access_userDocumentService } from './access_user-document.service';

describe('Service: Access_userDocument', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Access_userDocumentService]
    });
  });

  it('should ...', inject([Access_userDocumentService], (service: Access_userDocumentService) => {
    expect(service).toBeTruthy();
  }));
});
